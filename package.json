{
    "name": "equipment-tracker-root",
    "version": "1.0.0",
    "scripts": {
        "server": "npm run dev --prefix server",
        "client": "npm run dev --prefix client",
        "dev": "npm-run-all --parallel server client"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}